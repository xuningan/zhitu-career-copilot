{
  "name": "zhitu-career-copilot-mobile",
  "version": "0.3.0",
  "private": true,
  "description": "Android wrapper for 职途 · 自动化求职作战台",
  "scripts": {
    "android:add": "cap add android",
    "android:sync": "cap sync android",
    "android:build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}