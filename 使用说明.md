# 职途 · 自动化求职作战台

一个本地优先的个人求职工具，面向自动化、电气、PLC、工业机器人和智能制造方向。

当前版本：`v0.3`

## 快速启动

1. 双击桌面上的「职途 - 自动化求职作战台」快捷方式。
2. 或者双击项目目录中的 `启动求职台.cmd`。
3. 不需要安装 Node、Python、数据库或 Web 服务器。

如果浏览器没有自动打开，可以直接双击 `index.html`。建议使用最新版 Microsoft Edge 或 Google Chrome。

## v0.2 / v0.3 新增能力

### 1. 招聘平台搜索连接

在「岗位与匹配」页面，可以一键打开以下平台的公开搜索页：

- BOSS直聘
- 智联招聘
- 前程无忧
- 猎聘
- 实习僧
- 国聘

搜索关键词和目标城市来自你的「能力档案」。这里只做搜索跳转，不登录账号、不绕过验证码、不自动申请岗位。

### 2. 网页采集器和剪贴板导入

打开 `采集书签.html`，把「职途采集」按钮拖到浏览器书签栏。

使用流程：

1. 在 BOSS直聘、智联、前程无忧、猎聘等平台打开一个公开岗位详情页。
2. 点击浏览器书签栏里的「职途采集」。
3. 页面会把岗位标题、链接和页面文字复制到剪贴板。
4. 回到职途，点击「从剪贴板导入」。
5. 检查识别出的公司、岗位和 JD，再保存并计算匹配。

采集器只在你主动点击时读取当前页面文字。不同平台对页面复制和自动化有各自规则，因此本项目不提供自动登录、自动翻页、批量爬取或自动投递。

### 3. 自动筛选

岗位会按照以下条件自动标记为「推荐」「可考虑」或「不建议」：

- 关注关键词
- 排除关键词
- 最低岗位匹配分
- 目标城市
- 应届 / 经验要求

筛选规则在「数据与设置」或「岗位与匹配」中调整。自动筛选只处理已经导入职途的岗位，不会替你操作招聘平台。

### 4. 天眼查公司查询

在「公司查询」页面可以：

- 输入公司名称，打开天眼查官方搜索结果页。
- 从岗位详情或岗位卡片直接跳到对应公司的天眼查查询。
- 把公司经营状态、行业、规模、注册资本、法定代表人、风险等级和研究结论保存在本地。
- 把天眼查页面中能够公开看到的信息复制后粘贴到公司档案，系统会尽量自动提取字段。

本功能默认使用天眼查官方网页查询，不自动抓取登录后的数据、不绕过验证码，也不保存你的天眼查账号。若后续拥有天眼查开放平台正式授权 Token，可以再增加授权 API 数据同步。

### 5. AI 面试

打开「AI 面试」，选择目标岗位、面试风格和轮次，即可开始多轮追问。

支持多种模型来源：

- 自动检测：优先使用浏览器内置模型，否则尝试本地接口
- 浏览器内置模型：取决于浏览器和电脑是否支持
- Ollama / LM Studio：推荐本地隐私方案
- 兼容接口：支持 OpenAI 风格的 `chat/completions` 接口
- 离线训练模式：不使用 AI，只提供题库、回答提示和本地规则报告

#### Ollama 配置示例

安装 Ollama 后执行：

```powershell
ollama pull qwen2.5:7b
ollama serve
```

然后在职途的「数据与设置」中填写：

```text
模型来源：Ollama / 本地模型
接口地址：http://localhost:11434
模型名称：qwen2.5:7b
```

如果浏览器直接打开 HTML 时遇到跨域限制，需要根据所用模型服务的文档允许本机页面访问接口。没有连接成功时，AI 面试会自动切换为离线训练模式，不会伪装成 AI 评价。

## 第一次使用顺序

1. 打开「能力档案」，填写姓名、电话、邮箱、学校、目标岗位和目标城市。
2. 核对已经预填的 PLC、Factory IO、ABB、SolidWorks、UG 和减速器项目。
3. 打开「岗位与匹配」，删除示例岗位。
4. 使用平台搜索和采集器导入真实招聘 JD。
5. 调整自动筛选规则，查看推荐岗位和技能缺口。
6. 打开「简历工坊」，生成针对该岗位的简历初稿。
7. 在「投递看板」里手动更新状态和下一步动作。
8. 在「面试准备」保存固定题库，在「AI 面试」进行多轮模拟。
9. 面试后更新项目素材、回答和复盘记录。

## 数据与隐私

- 姓名、电话、邮箱、岗位记录和普通面试答案保存在当前浏览器的 LocalStorage 中。
- 云端 AI 接口会将面试内容发送给你选择的服务商，可能产生费用。
- 使用本地 Ollama / LM Studio 时，面试内容不会离开电脑。
- 更换浏览器、清理浏览器数据或重装系统前，请先在「数据与设置」中导出 JSON 备份。
- 当前版本没有职途云端服务器。

## 使用边界

- 匹配分不是录用概率，只是岗位关键词、技能档案和项目证据之间的关系。
- 系统不会捏造技能，缺失技能会明确标记。
- 生成的简历和面试内容必须由本人核实。
- 平台搜索链接可能随招聘网站改版而变化。
- AI 回答评价可能出错，只能作为面试练习参考。
- 不进行自动登录、绕过验证码、批量爬取或自动海投。
## 手机使用

GitHub Pages 地址：

https://xuningan.github.io/zhitu-career-copilot/

- Android Chrome / Edge：菜单 -> 添加到主屏幕 / 安装应用
- iPhone Safari：分享 -> 添加到主屏幕
- 手机和电脑的数据分别保存在各自浏览器中，可通过“设置”中的 JSON 备份导入导出同步

## Android APK

直接下载安装：

https://github.com/xuningan/zhitu-career-copilot/releases/download/android-v0.3.0/zhitu-career-copilot-android.apk
